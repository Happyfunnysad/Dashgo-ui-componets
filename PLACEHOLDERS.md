# Placeholder policy

The reusable SVG examples use neutral demonstration values such as `Selected item`, `Example value` and generic metrics.

The original Dashgo visual language is preserved:

- dimensions and spacing;
- dark palette and semantic colors;
- borders, radii and opacity levels;
- text hierarchy;
- reusable control and layout structure.

Replace placeholder labels when assembling a product-specific interface.
